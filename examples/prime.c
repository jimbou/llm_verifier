int fibonacci(int n) {
    //check that the input is positive
    if (n < 0) {
        return -1;
    }
    //find the nth fibonacci number
}
//the nth fibonacci number
// the input n should be positive or 0 and the result should be >=0 and >= n-1